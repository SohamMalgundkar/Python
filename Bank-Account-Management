class user():# class to get user details
  def __init__(self,name,age,gender):
    self.name=name
    self.age=age
    self.gender=gender
  def showdetails(self):
    print("personal details:-")
    print(f"Name is {self.name.title()}")
    print(f"Age is {self.age} ")
    print(f"Gender is {self.gender}")

class bank(user):#class to create bank acc
  __balance=0
  __usercount=0
  def __init__(self,name,age,gender):
    super().__init__(name,age,gender)
    bank.__usercount+=1
    self.accountno=f"denabank000{bank.__usercount}"
  
   def ud(self):
    return self.usercount

  def deposite(self, amount):# for processing deposite
   self.amount=amount
   self.__balance += self.amount
   print(f"account Balance is updated:-rs{self.__balance}")
 
 def withdraw(self,amount):#for processing withdrawl
   self.amount=amount
   if(self.amount>self.__balance):
    print(f"insufficient amount, balance available:-rs{self.__balance}")
   elif(100<=self.amount<=self.__balance):
    print("Thankyou for visiting")
    self.__balance-=self.amount
    print(f"current Balance is :- rs {self.__balance}")
   else:
    print("Cant withdraw amount less than rs.100")

  def viewbalance(self):
   self.showdetails()
   print(f"ac no :- {self.accountno}")
   print(f"Current balance is:- {self.__balance}")

  def transfer(self,username,amount):
   self.amount=amount
   if (self.amount >self.__balance):
    print(f"insufficient amount,balance available :- rs{self.__balance}")
   elif (1<=self.amount<=self.__balance):
    username.__balance+=self.amount
    self.__balance-=+self.amount
    print("amount Transfered Sucessfully")
    print(f"current balance is:-{self.__balance}")
   else:
    print("you cannot transfer less than rs. 1")
